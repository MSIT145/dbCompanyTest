@model IEnumerable<dbCompanyTest.ViewModels.ProductWallViewModel>



@section Style{
    <link href="~/css/ProductWall.css" rel="stylesheet">

}

<div class="words-statis">
    <div class="num">- 篩選條件 | <span>1</span> |</div>
    <a href="" class="clear-words">清除</a>
</div>
<div class="words-content">
    <div class="w-item">
        <div class="w-title">尺寸</div>
        <ul>
            @foreach (var item in Model){
                <li class="dpnone"><a href=""> @item.尺寸名稱 </a> <img src="/homeimg/checked.png" /></li>
            }
        </ul>
        <a href="" class="watch-more">+ 查看更多</a>
    </div>
    <div class="w-item">
        <div class="w-title">顏色</div>
        <ul>
            @foreach (var item in Model)
            {
                <li class="dpnone"><a href=""> @item.顏色名稱 </a> <img src="/homeimg/checked.png" /></li>
            }
        </ul>
        <a href="" class="watch-more">+ 查看更多</a>
    </div>
    <div class="w-item">
        <div class="w-title">顏色</div>
        <ul>
            @foreach (var item in Model)
            {
                <li class="dpnone"><a href=""> @item.材質名稱 </a> <img src="/homeimg/checked.png" /></li>
            }
        </ul>
        <a href="" class="watch-more">+ 查看更多</a>
    </div>
</div>

@model IEnumerable<dbCompanyTest.ViewModels.CarViewModels>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<hr />
<table class="table">

    <thead>
        <tr>
            <th>

            </th>
            <th>
                商品資訊
            </th>
            <th>
                @Html.DisplayNameFor(model => model.訂單數量)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.商品價格)
            </th>
        </tr>
        
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    <img src="~/images/@item.car圖片位置.商品圖片1" width="200" />

            </td>
                <td>
                    <dl class="row">

                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(modelItem => item.商品名稱)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(modelItem => item.商品名稱)
                        </dd>
                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(modelItem => item.尺寸種類)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(modelItem => item.尺寸種類)
                        </dd>
                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(modelItem => item.商品顏色種類)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(modelItem => item.商品顏色種類)
                        </dd>
                    </dl>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.訂單數量)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.商品價格)
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@item.Id">X</a>
            </td>
        </tr>
}
    </tbody>
</table>
